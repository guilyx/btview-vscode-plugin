version: 2
updates:
  - package-ecosystem: npm
    directory: /
    schedule:
      interval: weekly
    open-pull-requests-limit: 10
    ignore:
      # Removed from the project (pre-commit replaces Husky + lint-staged)
      - dependency-name: husky
      - dependency-name: lint-staged
